 
 import { createStitches } from '@stitches/react'
 
 
const { config, createTheme,css,getCssText,globalCss,keyframes,styled,theme} = createStitches({

  theme:{
    colors:{
      hayla:'#fffff'
    }
  }
})

export { getCssText }
